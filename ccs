#!/usr/bin/env python3
"""CCS 兼容入口，转发到共享核心逻辑。"""

import sys
import os

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from mms_runtime import ensure_supported_python

ensure_supported_python("CCS")

from mms_core import main

if __name__ == "__main__":
    main()
