{
  "name": "multi-model-switch",
  "version": "4.23.6",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "cd apps/runtime-api && python3 -m uvicorn main:app --reload --port 8000",
    "dev": "npm run dev:api",
    "build": "python3 -m py_compile lib/mms*.py lib/mmc*.py scripts/*.py",
    "type-check": "npm run build"
  }
}
