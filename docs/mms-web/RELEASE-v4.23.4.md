# v4.23.4 Stable · 文件夹浏览与真实调用路径回归

- 手机文件夹路径保持单行并显示尾部；展开较深目录时，每层截断提示标明目录，收起后不再残留。
- 补齐终端配置页监听器的反向 DNS 清扫，避免绑定局域网地址时长时间等待。
- Web 测试和 TypeScript 检查进入两条线 CI。删除、skip/xfail 不再被记为修好，失败后的重跑只有实际通过才清除回归。
- 补齐文件夹键盘、配置页工厂调用及真实事件接线测试。

## 升级须知

无配置或数据迁移。本版本保持4.x Stable；Bot功能继续属于5.x Preview。
