# v5.1.10 Preview · 保存模型设置后即时刷新

同步 Stable v4.23.7：管理通道模型保存默认 Effort 后，新任务立即刷新默认值和模型能力，无需刷新页面。保留单次任务与浏览器显式选择，已有会话不受影响。新增生产组件保存链路行为回归及断开接线 mutation 验证。

## 升级须知

更新会重启 Pilot；请先完成或停止正在执行、等待确认及排队的任务。升级后会重新加载页面，之后保存默认 Effort 即时生效。此次无需迁移模型、通道或 API Key；已有会话继续使用各自的 Effort。
