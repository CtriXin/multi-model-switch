# v4.19.5 · Pilot 首次配置与模型设置修复

## 升级须知

本版本可直接从 v4.19.x 升级。已有会话历史保留；正在运行的会话不会被升级流程强制清理。

## 修复

- 修复只安装过旧 MMS、从未安装 Pilot 4.x 的电脑首次配置后启动 Pi 时误报“独立 MMS 运行配置不存在”。首次连接可以使用已批准的模型目录创建独立运行快照。
- 取消选择全部模型时给出明确提示，避免保存一个无法启动的空通道。
- 统一模型设置与连接保存的确认弹窗，模型设置保存操作固定在顶部，长页面也能直接保存。
