# v4.22.1 · 修齐版本与安全更新

## 升级须知

本版本包含 v4.22.0 的全部 `/btw` 功能，并修复后端仍报 4.21.14 导致 Pilot 更新预检拒绝候选版本的问题。后端、npm manifests 和 Web 静态包统一为 4.22.1。无需迁移或清空配置、会话历史；正在运行的任务仍由既有安全更新流程等待处理。

## 修复

- 修正后端、前端与安装包版本不一致，确保安全更新能通过版本校验。
- 新增发布一致性回归，检查 runtime、根/Web manifests、lockfile、静态包哈希和对应更新日志；完整 fresh-user gate 会执行这项检查。
- 保留 v4.22.0 原 tag。它缺少后端版本 bump，不建议通过 Pilot 更新到该 tag；请使用 4.22.1。

`/btw` 同时用于终端 `mms pi` 和 Pilot，内建扩展仍为 0.59.0-fork.4。Windows 仍为 Native Preview；Bot 工作台仍在 5.0 预览线。
