# v4.23.3 · 输入焦点与模型选择反馈

- 打开模型与帮助搜索后可以直接输入；添加或编辑项目资料时，标题自动获得焦点。关闭窗口后返回原入口。
- 选择模型会等待保存成功再关闭，失败保留选择和错误提示。
- 当前会话调整 effort 时显示等待和失败状态，结果以 runtime 回读为准。

保持 4.x Stable 的现有页面与模型执行方式，无新配置或数据迁移。Bot/Fleet 交互属于 5.x Preview。
