# v4.23.7 Stable · 保存模型设置后即时刷新

在设置 → 管理通道模型保存默认 Effort 后，新任务会立即重新读取默认值和模型能力，无需刷新页面。保留用户单独指定的任务 Effort 与浏览器偏好；已有会话不受影响。

新增真实组件保存路径的行为回归，覆盖默认值刷新、显式覆盖保留及保存失败；断开保存回调或启动参数刷新接线时测试会失败。

## 升级须知

更新会重启 Pilot；请先完成或停止正在执行、等待确认及排队的任务。升级后会重新加载页面，之后保存默认 Effort 即时生效。此次无需迁移模型、通道或 API Key；已有会话继续使用各自的 Effort。
